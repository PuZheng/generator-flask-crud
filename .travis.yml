language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-flask-crud' ]; then cd .. && eval "mv $currentfolder generator-flask-crud" && cd generator-flask-crud; fi
